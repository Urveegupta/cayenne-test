package org.apache.cayenne.tutorial.persistent.auto;

import java.io.IOException;
import java.io.ObjectInputStream;
import java.io.ObjectOutputStream;
import java.time.LocalDate;

import org.apache.cayenne.BaseDataObject;
import org.apache.cayenne.exp.Property;
import org.apache.cayenne.tutorial.persistent.Departments;
import org.apache.cayenne.tutorial.persistent.Employees;

/**
 * Class _DeptManager was generated by Cayenne.
 * It is probably a good idea to avoid changing this class manually,
 * since it may be overwritten next time code is regenerated.
 * If you need to make any customizations, please use subclass.
 */
public abstract class _DeptManager extends BaseDataObject {

    private static final long serialVersionUID = 1L; 

    public static final String DEPT_NO_PK_COLUMN = "dept_no";
    public static final String EMP_NO_PK_COLUMN = "emp_no";

    public static final Property<LocalDate> FROM_DATE = Property.create("fromDate", LocalDate.class);
    public static final Property<LocalDate> TO_DATE = Property.create("toDate", LocalDate.class);
    public static final Property<Departments> DEPARTMENTS = Property.create("departments", Departments.class);
    public static final Property<Employees> EMPLOYEES = Property.create("employees", Employees.class);

    protected LocalDate fromDate;
    protected LocalDate toDate;

    protected Object departments;
    protected Object employees;

    public void setFromDate(LocalDate fromDate) {
        beforePropertyWrite("fromDate", this.fromDate, fromDate);
        this.fromDate = fromDate;
    }

    public LocalDate getFromDate() {
        beforePropertyRead("fromDate");
        return this.fromDate;
    }

    public void setToDate(LocalDate toDate) {
        beforePropertyWrite("toDate", this.toDate, toDate);
        this.toDate = toDate;
    }

    public LocalDate getToDate() {
        beforePropertyRead("toDate");
        return this.toDate;
    }

    public void setDepartments(Departments departments) {
        setToOneTarget("departments", departments, true);
    }

    public Departments getDepartments() {
        return (Departments)readProperty("departments");
    }

    public void setEmployees(Employees employees) {
        setToOneTarget("employees", employees, true);
    }

    public Employees getEmployees() {
        return (Employees)readProperty("employees");
    }

    @Override
    public Object readPropertyDirectly(String propName) {
        if(propName == null) {
            throw new IllegalArgumentException();
        }

        switch(propName) {
            case "fromDate":
                return this.fromDate;
            case "toDate":
                return this.toDate;
            case "departments":
                return this.departments;
            case "employees":
                return this.employees;
            default:
                return super.readPropertyDirectly(propName);
        }
    }

    @Override
    public void writePropertyDirectly(String propName, Object val) {
        if(propName == null) {
            throw new IllegalArgumentException();
        }

        switch (propName) {
            case "fromDate":
                this.fromDate = (LocalDate)val;
                break;
            case "toDate":
                this.toDate = (LocalDate)val;
                break;
            case "departments":
                this.departments = val;
                break;
            case "employees":
                this.employees = val;
                break;
            default:
                super.writePropertyDirectly(propName, val);
        }
    }

    private void writeObject(ObjectOutputStream out) throws IOException {
        writeSerialized(out);
    }

    private void readObject(ObjectInputStream in) throws IOException, ClassNotFoundException {
        readSerialized(in);
    }

    @Override
    protected void writeState(ObjectOutputStream out) throws IOException {
        super.writeState(out);
        out.writeObject(this.fromDate);
        out.writeObject(this.toDate);
        out.writeObject(this.departments);
        out.writeObject(this.employees);
    }

    @Override
    protected void readState(ObjectInputStream in) throws IOException, ClassNotFoundException {
        super.readState(in);
        this.fromDate = (LocalDate)in.readObject();
        this.toDate = (LocalDate)in.readObject();
        this.departments = in.readObject();
        this.employees = in.readObject();
    }

}
